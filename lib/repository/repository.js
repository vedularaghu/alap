class Repository {
    constructor(connection) {
        this.connection = connection;
    }

    persist(user) {}

    findById(id) {}

    findBy() {}

    remove(user) {}
}

module.exports = Repository;